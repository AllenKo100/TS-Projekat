@model IEnumerable<MovieHub.Models.WatchlistFilm>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Film)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Watchlist)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Film.Naziv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Watchlist.Naziv)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WatchlistFilmID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WatchlistFilmID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WatchlistFilmID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
